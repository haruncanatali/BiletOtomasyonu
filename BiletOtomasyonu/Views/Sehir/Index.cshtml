@model List<BiletOtomasyonu.Models.Sehir>
@{
    ViewBag.Title = "Index";
}

<h2 class="text-center h2 mt-5">Şehirler Sayfası</h2>
<hr />
@if (Model.Count == 0)
{
    <div class="alert alert-danger" role="alert">
        Sisteme kayıtlı şehir bulunamadı.
        <a href="/Sehir/Add" class="btn btn-primary">Ekle</a>
    </div>
}
else
{
    <div class="text-end" style="margin-right: 15px; margin-bottom: 5px;">
        <a href="/Sehir/Add" class="btn btn-primary">Ekle</a>
    </div>
    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>#</th>
                <th>Şehir Adı</th>
                <th>Veritabanı İşlemleri</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id.ToString()</td>
                    <td>@item.SehirAdi</td>
                    <td class="text-center">
                        <div class="row">
                            <div class="col-md-4">
                                <a class="btn btn-warning btn-sm" href="/Sehir/Update/@item.Id" style="width: 70px;">Güncelle</a>
                            </div>
                            <div class="col-md-4 d-flex justify-content-center">
                                <a class="btn btn-danger btn-sm" href="/Sehir/Delete/@item.Id" style="width: 100px;">Sil</a>
                            </div>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}

